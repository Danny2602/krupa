import api from "@/lib/axios";

export const doctorApi = {
    // getDoctorAppointments: async () => {
    //     const result = await api.get('/doctor/appointments');
    //     return result;
    // },
    // updateAppointmentStatus: async (appointmentId, status) => {
    //     const result = await api.patch(`/doctor/appointments/${appointmentId}`, { status });
    //     return result;
    // },
}
